# API base URL — include the /api prefix.
# Leave empty in development to use Vite's proxy (see vite.config.ts).
# In production, set this to your deployed backend URL.
VITE_API_URL=http://localhost:5000/api
